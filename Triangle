#include <stdio.h>
#include <math.h>

int main(){
  int a,b,c,x,y,z;
  if (a<=0 && b<=0 && c<=0)
  printf("invalid input\n");
  else
  {
  printf("Enter number : \n");
  scanf("%d", &a);
  printf("Enter number : \n");
  scanf("%d", &b);
  printf("Enter number : \n");
  scanf("%d", &c);
  }

 x=a+b;
 y=b+c;
 z=a+c;
 if (x<c)
 printf("It doesnt forms a triangle\n");
 
 else if (y<a)
 printf("It doesnt forms a triangle\n");
 
 else if (z<b)
 printf("It doesnt forms a triangle\n");
 
 else
 printf("It forms a triangle\n");
 
  return 0;
}
